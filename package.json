{
  "name": "newrubbish-uniapp",
  "version": "1.0.0",
  "description": "UNI-APP project for rubbish classification",
  "scripts": {
    "serve": "uni serve",
    "build:h5": "uni build --target h5",
    "build:mp-weixin": "uni build --target mp-weixin",
    "build:app-plus": "uni build --target app-plus"
  },
  "dependencies": {
    "vue": "^3.0.0",
    "pinia": "^2.0.0"
  },
  "devDependencies": {
    "uni-cli": "^2.0.0"
  }
}